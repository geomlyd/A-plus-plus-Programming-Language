function include (filepath) {
	source = fileOpen(filepath);

	code = fileRead(source);

	ast = @(code);
	
	return ast;
}

functionFactory = include("metaprogrammingTests/functionFactory.asc");

!(functionFactory);

print("\n");

objectFactory = include("metaprogrammingTests/objectFactory.asc");

!(objectFactory);
